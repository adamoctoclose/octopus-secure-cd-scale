@{
    ViewData["Title"] = "Home";
    // Fetch the secret from the environment variable
    var injectedPassword = Environment.GetEnvironmentVariable("PASSWORD") ?? "Password not found";
}

<!-- Page-specific content -->
<h1 class="display-4">Injected Secret</h1>

<p class="lead">
    The secret value is: <strong>@injectedPassword</strong>
</p>
